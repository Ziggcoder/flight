{
  "name": "flight-simulator",
  "version": "1.0.0",
  "private": true,
  "description": "Two-player motion-controlled flight simulator for Raspberry Pi with ESP32 remotes",
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "node scripts/check.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
